import React, {Fragment} from 'react';
import {Query} from 'react-apollo';
import {Link} from 'react-router-dom';

//import queries
import {EXERCISE_QUERY} from '../Queries/index'

const Contacts = () =>{
    
        
        return(
            <h1>Hola</h1>
        )
    
        }

export default Contacts;